language: none
fast_finish: true

os:
  - linux

env: FC=gfortran

dist: trusty
group: edge

notifications:
  email: false

git:
  depth: 3

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - gfortran-7

install:
    - gfortran -v
    - make

script:
    - ./stock_gain
    - ./stock_volatility
    - ./stock_crossover
